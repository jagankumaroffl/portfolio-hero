/**
 * Placeholder for a future scroll indicator.
 *
 * NOTE: the original hero had no scroll-indicator element, so this is not
 * imported by Hero.tsx — adding it to the render tree would change the UI.
 * Kept here only so the folder matches the requested structure; wire it up
 * and add markup/styles when the design actually calls for one.
 */
export default function ScrollIndicator() {
  return null;
}

/** Decorative grid lines + outline circle drawn above the hero imagery. */
export default function HeroBackground() {
  return (
    <div className="gh-grid-wrap" aria-hidden="true">
      <div className="gh-grid" />
      <div className="gh-circle" />
    </div>
  );
}
